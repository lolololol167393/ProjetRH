{% extends 'base.html.twig' %}

{% block title %}TypeConges{% endblock %}

{% block body %}
    <h1>TypeConges</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_conge.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_conge.nom }}</td>
            </tr>
            <tr>
                <th>Jours_an</th>
                <td>{{ type_conge.joursAn }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ type_conge.couleur }}</td>
            </tr>
            <tr>
                <th>Remuneration</th>
                <td>{{ type_conge.remuneration ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_conges_index') }}">back to list</a>

    <a href="{{ path('app_type_conges_edit', {'id': type_conge.id}) }}">edit</a>

    {{ include('type_conges/_delete_form.html.twig') }}
{% endblock %}
